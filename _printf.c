#include "main.h"
/**
 *
 *
 */
int _printf(const char *format, ...)
{
	int i = 0;
	va_list list;

	if (format)
	{
		if (format[0] == ('\"') || (format[0] == ('\''))
			{
				while (format[i] != '%')
				{
					

				}
			}
	}
}
